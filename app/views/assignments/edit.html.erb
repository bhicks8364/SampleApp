<h1>Editing assignment</h1>

<%= simple_form_for(@assignment) do |f| %>
  <% if @assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% @assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <%= f.fields_for :job_order %>
    <%= f.association :employee_profile %>
    <%= f.input :pay_rate, as: :decimal %>
    <%= f.input :bill_rate, as: :decimal %>

    <%= f.button :button, "Save".html_safe, data: {disable_with: "<i class='fa fa-spinner fa-spin'></i> Saving..."} %>

<% end %>



<%= link_to 'Show', @assignment %> |
<%= link_to 'Back', assignments_path %>
