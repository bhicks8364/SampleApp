<%= form_for(@assignment) do |f| %>
  <% if @assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% @assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee_profile_id %><br>
    <%= f.collection_select :employee_profile_id, EmployeeProfile.all, :id, :employee_name %>
  </div>
  <div class="field">
    <%= f.label :job_order_id %><br>
    <%= f.grouped_collection_select :job_order_id, CompanyProfile.all, :job_orders, :company_name, :id, :job_title, include_blank: true %>
  </div>
  
  
  <div class="field">
    <%= f.label :pay_rate %><br>
    <%= f.text_field :pay_rate %>
  </div>
  <div class="field">
    <%= f.label :bill_rate %><br>
    <%= f.text_field :bill_rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


