#app = angular.module("Raffler", ["ngResource"])
#
#@AboutCtrl = ($scope, $resource) ->
    #EmployeeProfile = $resource("employee_profiles/:id", {id: "@id"})
    #$scope.entries = [
        #{name: "Larry"}
        #{name: "Curly"}
        #{name: "Moe"}
    #]
    #
    #$scope.addEntry = ->
        #$scope.entries.push($scope.newEntry)
        #$scope.newEntry = {}